# SlingCheck - AI-проверка эргономики слинга

MVP приложение для анализа правильности положения ребенка в слинге или рюкзаке-переноске с использованием AI Vision API (Claude или GPT-4).

## Возможности

- Загрузка фото через drag-and-drop или выбор файла
- Выбор между Claude 3.5 Sonnet, Claude Sonnet 4.5, GPT-4 Turbo и GPT-4o
- Анализ эргономики положения ребенка
- Конкретные рекомендации по исправлению ошибок
- Адаптивный дизайн для PC и смартфонов
- Хранение API ключей в браузере (localStorage)

## Структура проекта

```
sling-check/
├── index.html           # Главная страница
├── css/
│   └── style.css       # Стили приложения
├── js/
│   ├── config.js       # Конфигурация моделей и API
│   ├── api.js          # API клиент для Claude/OpenAI
│   └── app.js          # Основная логика приложения
├── config/
│   └── prompts.json    # Экспертные промпты для анализа
└── assets/
    └── icons/          # Иконки (опционально)
```

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/sling-check.git
cd sling-check
```

### 2. Получение API ключа

Вам понадобится API ключ от одного из провайдеров:

**Claude (Anthropic):**
- Зарегистрируйтесь на https://console.anthropic.com/
- Создайте API ключ в разделе API Keys
- Модель: `claude-3-5-sonnet-20241022`

**OpenAI:**
- Зарегистрируйтесь на https://platform.openai.com/
- Создайте API ключ в разделе API Keys
- Модели: `gpt-4-turbo` или `gpt-4o`

### 3. Локальный запуск

#### Простой способ (рекомендуется):

```bash
./start.sh
```

Этот скрипт автоматически запустит:
- CORS прокси-сервер на порту 8002
- Веб-сервер на порту 8001

**На компьютере:**
Откройте http://localhost:8001 в браузере.

**На мобильном устройстве (в той же WiFi сети):**
1. Узнайте IP адрес компьютера:
   - Windows: `ipconfig` (смотрите IPv4 Address)
   - Linux/Mac: `ip addr` или `ifconfig` (смотрите inet)
2. Откройте `http://<IP-адрес>:8001` на мобильном
   - Например: `http://192.168.1.100:8001`

Для остановки:
```bash
./stop.sh
```

#### Ручной запуск:

1. **Запустите CORS прокси-сервер** (решает проблему CORS):
```bash
python3 proxy-server.py &
```

2. **Запустите веб-сервер**:
```bash
python3 -m http.server 8001
```

3. Откройте http://localhost:8001 в браузере.

### 4. Использование

1. Выберите модель AI (Claude или GPT-4)
2. Введите API ключ и нажмите "Сохранить"
3. Загрузите фото ребенка в слинге/рюкзаке
4. Нажмите "Анализировать"
5. Получите детальную оценку и рекомендации

## Конфигурация

### Изменение промптов

Отредактируйте `config/prompts.json` для настройки экспертного анализа:

```json
{
  "slingAnalysis": {
    "system": "Системный промпт...",
    "userPrompt": "Инструкции для анализа...",
    "examples": [...]
  }
}
```

### Добавление новых моделей

В `js/config.js` добавьте новую модель в объект `CONFIG.models`:

```javascript
yourModel: {
  name: 'Model Name',
  id: 'model-id',
  provider: 'anthropic', // или 'openai'
  maxTokens: 4096,
  supportsVision: true,
  endpoint: 'https://api.example.com/v1/...',
  apiKeyStorageKey: 'slingcheck_yourmodel_api_key'
}
```

## Безопасность

- API ключи хранятся только в localStorage браузера
- Ключи не передаются на сторонние серверы (кроме API провайдеров)
- Изображения обрабатываются локально и отправляются напрямую в API
- Рекомендуется использовать на доверенных устройствах

## Технические детали

- **Фронтенд:** Vanilla JavaScript (ES6+)
- **Стили:** CSS3 с CSS Variables
- **AI API:** Anthropic Claude / OpenAI GPT-4
- **CORS Proxy:** Python HTTP сервер (решает проблему CORS)
- **Хранилище:** localStorage (браузер)
- **Максимальный размер изображения:** 5MB
- **Поддерживаемые форматы:** JPG, PNG, WebP

### Архитектура

```
Браузер → Веб-сервер (8001) → SlingCheck App
                ↓
         CORS Proxy (8002) → Anthropic/OpenAI API
```

**CORS Proxy** решает проблему CORS (Cross-Origin Resource Sharing):
- Перехватывает запросы к AI API
- Добавляет необходимые CORS заголовки
- Перенаправляет запросы к реальным API
- Возвращает ответы клиенту

## Деплой на GitHub Pages

### Требуется Cloudflare Worker!

Anthropic и OpenAI API **НЕ поддерживают CORS** из браузера, поэтому для GitHub Pages нужен прокси-сервер.

**Шаги:**

1. **Создайте Cloudflare Worker** (бесплатно, 5 минут)
   - Следуйте подробной инструкции: [CLOUDFLARE_SETUP.md](CLOUDFLARE_SETUP.md)
   - Скопируйте URL вашего Worker

2. **Обновите config.js**
   - Откройте `js/config.js`
   - Найдите строку `workerUrl: 'https://slingcheck-proxy.your-subdomain.workers.dev'`
   - Замените на URL вашего Worker

3. **Создайте репозиторий и задеплойте**
   ```bash
   git add .
   git commit -m "Add Cloudflare Worker support"
   git push
   ```

4. **Включите GitHub Pages**
   - Settings → Pages → Source: `main` branch → Save

**Готово!** Приложение будет работать на любом устройстве! 🎉

**Стоимость:** Cloudflare Worker бесплатен (100,000 запросов/день)

## Ограничения MVP

- Нет серверной части (все в браузере)
- API ключи хранятся в localStorage
- Нет истории анализов
- Нет возможности сравнения фото
- Нет мультиязычности (только русский)

## Развитие проекта

Возможные улучшения:
- [ ] Добавить историю анализов
- [ ] Сравнение до/после
- [ ] Мультиязычность (английский, испанский)
- [ ] Темная тема
- [ ] Экспорт результатов в PDF
- [ ] Видео-анализ
- [ ] Интеграция с Telegram Bot

## UPDATES

# Улучшенный промпт для анализа слингоношения v2.0

### Проблемы старого промпта:

1. **Слишком общий** — "M-позиция (поза лягушки)" — это что конкретно? Модель может интерпретировать по-разному.

2. **Нет структуры для vision-анализа** — модель не знает, ЧТО именно искать на изображении визуально.

3. **Нет приоритизации** — все критерии равнозначны, хотя безопасность дыхания критичнее, чем идеальный угол бёдер.

4. **Не учитывает возраст** — требования для новорожденного и 8-месячного ребёнка разные.

5. **Не учитывает тип слинга** — шарф и эргорюкзак имеют разные особенности.

6. **Нет обработки неопределённости** — что делать, если на фото что-то не видно?

---

## Ключевые улучшения в v2.0

### 1. Основа на международных стандартах

Промпт построен на двух признанных системах:

- **T.I.C.K.S.** (UK Sling Consortium) — 5 критериев безопасности:
  - **T**ight — плотное прилегание
  - **I**n view — видимость лица
  - **C**lose enough to kiss — правильная высота
  - **K**eep chin off chest — подбородок не прижат к груди
  - **S**upported back — поддержка спины

- **IHDI** (International Hip Dysplasia Institute) — рекомендации по M-позиции:
  - Бёдра разведены под углом 60-120°
  - Колени выше попы
  - Поддержка бёдер от колена до колена

### 2. Конкретные визуальные маркеры

Вместо абстрактного "колени выше попы" — конкретное описание того, что модель должна увидеть:

```
**Что искать визуально:**
- Колени находятся ВЫШЕ уровня попы
- Бедра разведены в стороны (угол между бедрами примерно 60-120°)
- Колени согнуты примерно под углом 90° или больше
- Ткань/сиденье поддерживает бедра от колена до колена

**Признаки проблемы:**
- Ноги свисают вниз (hanging legs)
- Колени ниже попы или на одном уровне
- Узкое сиденье — ткань не доходит до подколенных ямок
```

### 3. Иерархия критичности

- 🔴 **КРИТИЧНО** — риск для жизни (дыхание, падение)
- 🟡 **ВАЖНО** — влияет на здоровье (эргономика суставов)
- 🟢 **ЖЕЛАТЕЛЬНО** — комфорт

### 4. Структурированный вывод

Чёткий формат ответа:
- Общая оценка (1-10)
- Статус (безопасно / требует корректировки / критично)
- Что хорошо
- Что требует внимания
- Конкретные рекомендации
- Что не удалось оценить по фото

### 5. Возрастная дифференциация

Разные требования для:
- 0-4 месяца — только лицом к родителю, полная поддержка головы
- 4-6 месяцев — переходный период
- 6+ месяцев — больше вариантов позиций

### 6. Обработка неопределённости

Модель должна честно указывать:
- ❓ "не видно на фото"
- Рекомендации сделать дополнительные фото с других ракурсов

---

## Рекомендации по внедрению

### 1. Тестирование

Перед запуском протестируйте на разных типах фото:
- ✅ Правильная позиция
- ⚠️ С незначительными проблемами
- ❌ С явными нарушениями
- 📷 Плохое качество / неудачный ракурс

### 2. Валидация экспертами

Необходимо у экспертов оценить новые результаты. Собрать:
- False positives (модель видит проблему, где её нет)
- False negatives (модель пропускает проблему)
- Качество рекомендаций

### 3. Итеративные улучшения

После тестирования можно добавить:
- Примеры изображений (few-shot learning)
- Специфику для конкретных моделей слингов
- Локализованные рекомендации (где купить, кого консультировать)

### 4. Дисклеймер

Обязательно добавить в приложение:
```
Этот анализ не заменяет консультацию сертифицированного 
слингоконсультанта. При любых сомнениях обратитесь к специалисту.
```

---

## Техническая интеграция

### Пример вызова API с новым промптом:

```javascript
const response = await anthropic.messages.create({
  model: "claude-sonnet-4-20250514",
  max_tokens: 4096,
  messages: [
    {
      role: "user",
      content: [
        {
          type: "image",
          source: {
            type: "base64",
            media_type: "image/jpeg",
            data: imageBase64,
          },
        },
        {
          type: "text",
          text: slingAnalysisPrompt.userPrompt
        }
      ],
    }
  ],
  system: slingAnalysisPrompt.system
});
```

### Постобработка ответа

Можно парсить структурированный ответ для:
- Извлечения общей оценки
- Выделения критических предупреждений
- Формирования push-уведомлений при опасных ситуациях

---

## Источники

1. UK Sling Consortium — T.I.C.K.S. Guidelines: https://babyslingsafety.co.uk/
2. International Hip Dysplasia Institute — Babywearing: https://hipdysplasia.org/baby-wearing/
3. Babywearing International — Safety Standards: https://babywearinginternational.org/
4. Ergobaby — Positioning Guidelines: https://ergobaby.com/
