# Настройка Cloudflare Worker для SlingCheck

Cloudflare Worker нужен для обхода CORS ограничений при работе на GitHub Pages.

## Шаги настройки:

### 1. Создайте аккаунт на Cloudflare

1. Перейдите на https://dash.cloudflare.com/sign-up
2. Зарегистрируйтесь (бесплатно)

### 2. Создайте Worker

1. Войдите в https://dash.cloudflare.com/
2. Выберите **Workers & Pages** в левом меню
3. Нажмите **Create application**
4. Выберите **Create Worker**
5. Дайте имя worker (например: `slingcheck-proxy`)
6. Нажмите **Deploy**

### 3. Настройте код Worker

1. После создания нажмите **Edit code**
2. Удалите весь код в редакторе
3. Скопируйте и вставьте код из файла `cloudflare-worker.js`
4. Нажмите **Save and Deploy**

### 4. Получите URL Worker

После деплоя вы увидите URL типа:
```
https://slingcheck-proxy.your-subdomain.workers.dev
```

Скопируйте этот URL!

### 5. Обновите config.js

Откройте файл `js/config.js` в вашем проекте и найдите строку:

```javascript
// URL вашего Cloudflare Worker (замените на свой!)
workerUrl: 'https://slingcheck-proxy.your-subdomain.workers.dev',
```

Замените `'https://slingcheck-proxy.your-subdomain.workers.dev'` на ваш URL Worker.

### 6. Закоммитьте и запушьте на GitHub

```bash
git add js/config.js
git commit -m "Add Cloudflare Worker URL"
git push
```

Подождите 1-2 минуты пока GitHub Pages обновится.

### 7. Готово!

Теперь приложение будет работать на любом устройстве через GitHub Pages! 🎉

## Стоимость

- **Бесплатный tier**: 100,000 запросов в день
- Этого более чем достаточно для личного использования

## Безопасность

- Worker проверяет origin запросов
- API ключи передаются через Worker, но не сохраняются
- Worker только перенаправляет запросы, не логирует данные

## Troubleshooting

Если не работает:

1. Проверьте что Worker задеплоен: зайдите на URL Worker в браузере - должен быть ответ "Method Not Allowed"
2. Проверьте что URL в config.js правильный
3. Очистите кеш браузера (Ctrl+Shift+R)
4. Проверьте консоль браузера (F12) на ошибки
